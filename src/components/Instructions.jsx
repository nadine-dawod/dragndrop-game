import "./Instructions.css";

export const Instructions = () => {
    return (
      <div className="instructions">
        <p>Drag and drop the elements onto the canvas below to build</p>
      </div>
    );
  };